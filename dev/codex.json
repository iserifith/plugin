{
  "run": [{
    "id": "run",
    "method": "shell.run",
    "params": {
      "message": "npx -y @openai/codex {{args.prompt ? JSON.stringify(args.prompt) : ''}}",
      "path": "{{args.cwd}}",
      "input": true
    }
  }]
}
